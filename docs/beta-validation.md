# Work Tempo 2.3.3-beta.1 本机验证记录

日期：2026-09-09。应用版本2.3.3，构建1258，arm64；临时签名，未公证。本记录是公开前的本机检查结果。维护者后续授权直接公开Beta，不代表下列跨机待验收项已通过。

## 已执行

| 检查 | 结果 |
| --- | --- |
| 独立源码快照、Release构建 | 通过；Xcode 26.6，未替换当前安装 |
| 主工程Debug单元测试 | 102通过，0失败、0跳过 |
| 快捷启动模块全量测试 | 315通过，0失败 |
| 模块图标确定性复验 | 28通过，0失败（全量测试子集，不重复计数） |
| 快捷启动交付验证 | 11步完成，包含第二宿主契约验证及Debug/Release构建；HostDemo不进入安装包 |
| 工程Python回归 | 27通过 |
| DMG完整性及SHA-256 | 通过 |
| 只读挂载、复制应用、弹出镜像 | 通过 |
| 复制后嵌套代码签名完整性 | 通过；仅临时签名，不等于Developer ID认证 |
| 复制后测试模式启动两次 | 两次均存活5秒，由验证脚本退出；不启用真实业务服务 |
| 当前已安装软件保护 | 可执行文件、Info.plist哈希及进程ID前后一致 |

DMG SHA-256：`717136272e1450c293cb35f69c435ba671f9458b0628ac2662dbc63490222c22`。

源码快照包含当前“录屏状态不应锁住打开入口”和“重新打开应用恢复面板”的未提交修复。原工作区改动保持原状，尚未上传公开仓库。

## 尚未验证，不能标为通过

- 正常开启Gatekeeper的其他Mac首次下载安装：本机系统显示`assessments disabled`，并非本轮更改，因此本机安全评估返回accepted没有跨机参考效力。
- 正常用户启动后的真实鼠标/全局快捷键、权限弹窗、日历、截图录屏、文件授权与重启恢复：需要朋友按照安装说明逐项操作。测试模式启动不会执行这些功能。
- 最低macOS14.6实机兼容：当前测试系统是macOS26.5.2；Intel机型未验证、不在本包支持范围。
- 本报告生成时公网网页与下载尚未开放；之后的发布与网络验证结果以GitHub Release及官网为准，不混入本机检查结果。
- Developer ID签名、苹果公证和自动更新：尚未完成；stapler未发现公证票据。

交付目录包含DMG、对应源码ZIP、安装说明、release-manifest.json、SHA256SUMS.txt及local-validation.json。安装包内部说明和构建清单保留打包时的私测试用记录；以当前Release说明为准。公开后仍需收集设备系统版本与逐项测试结果。
